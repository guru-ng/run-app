# Todo — live task list

Check and update this every session. Delete finished items rather than
leaving them checked — git history is the permanent record, this file is
just "what's left."

- [ ] Build gamification #1 (see PLAN.md "Up next: Gamification"): a
      confetti/flash animation on `LogRunForm.tsx`'s success path — pure
      CSS, no new dependency
- [ ] Build gamification #2: personal-best callouts ("longest run yet",
      "3rd run this week") computed from `runs`, surfaced right when a run
      is logged — new `src/lib/personalBests.ts`
